plugins {
    id 'nva.person.preferences.java-conventions'
}

dependencies {
    testRuntimeOnly(nvaCatalog.datafaker)
    testRuntimeOnly(nvaCatalog.jackson.core)
    testRuntimeOnly(nvaCatalog.jackson.databind)
}
